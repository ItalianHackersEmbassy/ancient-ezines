uses graph;

var
  gd,gm : integer;
  
begin

  gd := ega;
  gm := egahi;
  initgraph(gd,gm,'');
  readln;
  closegraph;

  gd := cga;
  gm := cgac0;
  initgraph(gd,gm,'');
  readln;
  closegraph;
  
end.
